# Copy to webhook.env (gitignored) and set a long random secret.
# This MUST match the secret you set on the GitHub webhook.
# generate: openssl rand -hex 32
CODEOUTBOX_WEBHOOK_SECRET=CHANGE_ME_long_random_hex
